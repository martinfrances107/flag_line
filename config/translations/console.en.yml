command:
    flag_line:
        startTrains:
            description: 'Begin train services as defined by the run.'
            arguments:
                run_id: 'The run to use.'
        openStations:
            description: 'Begin train services as defined by the run.'
            arguments:
                run_id: 'The run to use.'
